<view class="content-card">
  <view class="card-top" wx:if='{{ isTitle }}'>
    <view class="card-dot" wx:if='{{ icon }}'></view>
    <view class="content-name">{{name}}</view>
  </view>
  <view class="card-bottom l-content {{cardPadding?'card-padding':'padd'}}">
    <slot />
  </view>
</view>